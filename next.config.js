module.exports = {
  i18n: {
    locales: ['en', 'es'],
    defaultLocale: 'en',
    localeDetection: false,
  },

  /**
   * config to work with netlify in serverless
   */
  target: 'serverless',
}
